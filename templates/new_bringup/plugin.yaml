# ═══════════════════════════════════════════════════════════
#  ROS2 接口契约（集成包）
#
#  ⚠ subpackages 必须替换为实际集成的子包名（至少 1 个）。
#    CI 会校验每个 subpackage 在 src/ 下存在。
# ═══════════════════════════════════════════════════════════

type: bringup

subpackages:
  - "TODO_replace_me"
